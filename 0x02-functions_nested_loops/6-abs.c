#include "main.h"


/**
 *  * _abs - check the code 
 *  * @num: Holds an integer
 *   * Return: Always 0.
 *    */
int _abs(int num)
{
		if (num < 0)
					return (num * -1);
			else
						return (num);
}
